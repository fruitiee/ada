def merge_sort(arr): 
if len(arr) > 1: 
mid = len(arr) // 2 
left = arr[:mid] 
right = arr[mid:] 
merge_sort(left) 
merge_sort(right) 
arr[:] = sorted(left + right) 
arr = [52, 37, 63, 14, 17, 8, 6, 25] 
merge_sort(arr) 
print("sorted array:", arr)
